name: delivery_app
description: Aplicación móvil simple de pedidos a domicilio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^12.1.3
  url_launcher: ^6.2.2
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  intl: ^0.19.0
  # Firebase dependencies - CONJUNTO COMPATIBLE SIN BUGS
  firebase_core: 2.13.0
  firebase_auth: 4.6.1
  cloud_firestore: 4.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/